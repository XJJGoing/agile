// 目前测试来说好像不管是自定义组件模式还是非自定义组件模式都能获取布局信息，所以如果没有问题还是不用改了
export default {
	// #ifdef APP-PLUS
	usingComponents: true,	// 是否使用自定义组件模式
	// #endif
	// #ifdef MP-WEIXIN
	usingComponents: true,	// 是否使用自定义组件模式
	// #endif
	// #ifdef MP-ALIPAY
	usingComponents: true,	// 是否使用自定义组件模式
	// #endif
	// #ifdef MP-BAIDU
	usingComponents: true,	// 是否使用自定义组件模式
	// #endif
	// #ifdef MP-TOUTIAO
	usingComponents: true,	// 是否使用自定义组件模式
	// #endif
	// #ifdef H5
	usingComponents: true,	// 是否使用自定义组件模式
	// #endif
}